export class Product {
    constructor(item, quantity, price) {
        this.item = item; // Name of the product
        this.quantity = quantity; // Quantity available
        this.price = price; // Price per unit
    }
}